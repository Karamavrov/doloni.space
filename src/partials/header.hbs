<!DOCTYPE html>
<html lang="{{lang}}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="{{description}}">
    
    <!-- Open Graph -->
    <meta property="og:title" content="{{title}}">
    <meta property="og:description" content="{{description}}">
    <meta property="og:url" content="{{canonical}}">
    <meta property="og:type" content="website">
    
    <!-- Canonical -->
    <link rel="canonical" href="{{canonical}}">
    
    <!-- Favicon -->
    <link rel="icon" type="image/png" href="/images/favicon.svg">
    
    <title>{{title}}</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/css/custom.css">
</head>
<!-- Google tag (gtag.js) -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-KBN3PHMVRQ"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());

  gtag('config', 'G-KBN3PHMVRQ');
</script>
<body>
    <header class="site-header py-3 print-hide">
        <div class="container">
            <div class="d-flex justify-content-between align-items-center">
                <a href="{{#if (eq lang 'en')}}/en/{{else}}/{{/if}}" class="text-decoration-none d-flex align-items-center gap-2">
                    <img src="/images/doloni_logo_header.svg" alt="Doloni Space" class="site-logo" >
                </a>
                <nav class="d-flex align-items-center gap-3">
                    <!-- Language Switcher -->
                    <div class="lang-switcher">
                        {{#if (eq lang 'uk')}}
                            <span class="fw-bold">UA</span>
                            <span>|</span>
                            <a href="{{#if (eq currentPage 'home')}}/en/{{else}}/en/{{currentPage}}{{/if}}" class="text-decoration-none">EN</a>
                        {{else}}
                            <a href="{{#if (eq currentPage 'home')}}/{{else}}/{{currentPage}}{{/if}}" class="text-decoration-none">UA</a>
                            <span>|</span>
                            <span class="fw-bold">EN</span>
                        {{/if}}
                    </div>
                </nav>
            </div>
        </div>
    </header>

